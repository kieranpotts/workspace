= Workspace

This repository encapsulates all my personal code repositories as Git submodules, along with shell scripts and a VS Code workspace configuration to manage them.

== Requirements

https://git-lfs.com/[Git LFS] SHOULD be installed before cloning the repositories. This is required to download PDFs and other large files from some repositories – but the `git clone` operation will succeed without it.

It is RECOMMENDED to clone and run the environment https://github.com/kieranpotts/bootstrap[bootstrapping script] first. This will install Git LFS and other dependencies of the repositories.

== Usage

NOTE: On Windows, the workspace SHOULD be established in WSL. The `devtools` repository SHOULD also be cloned directly in the host OS, manually, and optionally the `dotfiles` repository too.

[source,sh]
----
# Create an empty workspace root.
mkdir /path/to/workspace
cd /path/to/workspace

# Clone the workspace repository with all its submodules.
git clone --recursive git@github.com:kieranpotts/workspace.git .
----

If the workspace repository is already cloned, but you forgot to do it recursively, you can use the `install.sh` script to clone all the submodules.

[source,sh]
----
bash ./run/install.sh
----

The `sync.sh` script will synchronize the default branches of all submodules with their tracked upstream branches.

[source,sh]
----
bash ./run/sync.sh
----

All repositories are cloned into the `repos/` subdirectory as Git submodules.

To add a new repository to the workspace, use the following command. The `-b` flag checks out the tracked branch, while the `--force` flag allows adding to a non-empty directory.

[source,sh]
----
branch="dev"
url="git@github.com:kieranpotts/repository-name.git"
path="repos/repository-name"

git submodule add --force -b "${branch}" "${url}" "${path}"
----

To remove submodules:

[source,sh]
----
git rm --cached "{path}"
----

Open the VS Code workspace:

[source,sh]
----
code personal.code-workspace
----

''''

Copyright © 2020-present Kieran Potts, all rights reserved.
